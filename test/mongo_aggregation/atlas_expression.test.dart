import 'package:test/test.dart';

void main() {
  group('Atlas expression', () {
    test('add', () {
      //expect($expr(TestExpr()).build(), {'\$expr': '\$field'});
    });
    test('constant', () {
      //expect($expr(TestExpr()).build(), {'\$expr': '\$field'});
    });
    test('gaussian', () {
      //expect($expr(TestExpr()).build(), {'\$expr': '\$field'});
    });
    test('log', () {
      //expect($expr(TestExpr()).build(), {'\$expr': '\$field'});
    });
    test('log1p', () {
      //expect($expr(TestExpr()).build(), {'\$expr': '\$field'});
    });
    test('multiply', () {
      //expect($expr(TestExpr()).build(), {'\$expr': '\$field'});
    });
    test('path', () {
      //expect($expr(TestExpr()).build(), {'\$expr': '\$field'});
    });
    test('score', () {
      //expect($expr(TestExpr()).build(), {'\$expr': '\$field'});
    });
  });
}
